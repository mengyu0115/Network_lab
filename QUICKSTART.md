# 快速开始

## 1. 安装依赖

```bash
pip install -r requirements.txt
```

## 2. 启动 Web 平台

```bash
streamlit run web/app.py
```

浏览器默认访问：

```text
http://localhost:8501
```

## 3. 运行主线实验

1. 在侧边栏选择目标类型：`CLIP 图文对齐` 或 `BLIP 图像描述`
2. 选择对应的目标模型
3. 选择攻击方法：`FGSM` 或 `PGD`
4. 上传图片
5. 填写源文本和目标文本
6. 点击“开始多模态攻击”
7. 查看原图、对抗图、扰动图、统一判定和指标结果

## 4. 结果分析

“结果分析”页当前包含三类能力：

- 统一判定：固化展示 CLIP / BLIP 是否达成攻击阈值
- CLIP 验证：图文相似度变化
- BLIP 验证：原图描述 vs 对抗图描述
- 交叉评测：对同一对抗图在另一种多模态模型上复核

## 5. 样本管理

“样本管理”页支持：

- 批量上传图像
- 编辑标签 / 备注 / 分组
- 创建样本集合版本快照
- 导出样本清单 CSV / JSON
- 导出实验索引 CSV / JSON

## 6. 历史记录

平台会自动保存实验结果到 `data/adversarial/`，并在“历史记录”页提供：

- 实验筛选
- 指标查看
- 统一成功判定
- 样本索引浏览
- 原图 / 对抗图 / 扰动图对比
- 已执行的 CLIP / BLIP 多模态验证结果
